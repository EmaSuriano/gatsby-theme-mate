{
  "name": "gatsby-starter-theme-workspace",
  "private": true,
  "version": "0.0.0",
  "main": "index.js",
  "license": "0BSD",
  "scripts": {
    "clean": "yarn workspace example clean",
    "start": "env-cmd yarn workspace example start",
    "build": "env-cmd yarn workspace example build",
    "test": "env-cmd yarn workspace example test:ci",
    "check-types": "yarn workspace gatsby-theme-mate check-types",
    "release": "yarn workspace gatsby-theme-mate release"
  },
  "workspaces": [
    "gatsby-theme-mate",
    "example"
  ],
  "devDependencies": {
    "env-cmd": "^10.1.0"
  }
}
